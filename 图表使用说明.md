# 图表使用说明

## ✅ 已生成的图表

所有示例图表已生成在 `thesis_charts/` 目录下：

1. **fold_change_example.png** - Fold Change 示例图表
2. **lsmean_control_example.png** - 对照组 LSMean 示例图表
3. **lsmean_treatment_example.png** - 处理组 LSMean 示例图表
4. **go_summary_example.png** - GO 主题汇总示例图表
5. **go_theme_detail_example.png** - GO 主题详细示例图表
6. **system_architecture_example.png** - 系统架构图

## 📖 在 Markdown 中查看图表

### 方法 1：在 Cursor/VS Code 中预览
1. 打开 `Thesis_Defense_Document.md`
2. 按 `Ctrl+Shift+V` 打开预览
3. **图表现在应该可以正常显示了！** ✅

### 方法 2：使用 Markdown Preview Enhanced 扩展
1. 安装扩展：**Markdown Preview Enhanced**
2. 打开文档，右键 → "Markdown Preview Enhanced: Open Preview to the Side"
3. 图表会以更高质量显示

## 🔄 重新生成图表

如果需要重新生成图表，运行：

```bash
python generate_example_charts.py
```

## 📝 替换为真实图表

如果你想使用项目生成的真实图表：

1. **运行项目**：
   ```bash
   # 启动后端
   python start_backend.py
   
   # 启动前端（另一个终端）
   cd genegen
   npm run dev
   ```

2. **生成真实图表**：
   - 访问 http://localhost:3000/gene-search
   - 搜索一个基因（如 GAPDH）
   - 点击"Show Fold Change"等按钮生成图表
   - 右键保存图片

3. **替换示例图表**：
   - 将真实图表保存到 `thesis_charts/` 目录
   - 替换对应的示例图表文件名
   - 或更新 Markdown 中的图片路径

## 💡 提示

- 图表路径使用相对路径：`thesis_charts/图表名.png`
- 确保 `thesis_charts/` 目录和 Markdown 文件在同一目录下
- 如果图表不显示，检查路径是否正确

## 🎨 图表说明

- **示例图表**：使用模拟数据生成，用于演示系统功能
- **真实图表**：从实际项目运行中生成，包含真实数据
- 所有图表都是 300 DPI 高分辨率，适合打印和演示

