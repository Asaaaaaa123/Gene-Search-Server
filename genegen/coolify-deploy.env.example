# Copy to coolify-deploy.env for Coolify-only frontend deploys, or pass build ARGs (see root .env.example).
# Line format: KEY=value (no spaces around =). Use LF line endings, not Windows CRLF.
#
# Docker Compose (full stack): use root .env — NEXT_PUBLIC_API_URL=http://backend:8000
# Split-domain production: NEXT_PUBLIC_API_URL=https://your-api.example.com

NEXT_PUBLIC_API_URL=https://your-api.example.com

# Clerk — same publishable key in both lines
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx
